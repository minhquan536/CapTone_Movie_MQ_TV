import { styled } from "styled-components"

export const fromInfo = styled.div`
    display: grid;
    grid-template-columns: 1fr 1fr;

    img{
        width: 100%;
        height: 100%
        object-fit: cover;
    }
`