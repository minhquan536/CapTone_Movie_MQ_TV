import { styled } from "styled-components"

export const form_r = styled.div`

    display: grid;
    justify-content: right;
    padding-right: 5rem;
    img{
        height: 5rem;
        object-fit:cover ;
    }
`